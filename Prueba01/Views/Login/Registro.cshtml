@model Prueba01.Models.Usuario

@{
    ViewBag.Title = "Registro";
}

<h2>Crear Cuenta</h2>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">
        @ViewBag.Error
    </div>
}

@using (Html.BeginForm("Registro", "Login", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Ingrese sus datos</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            <label class="control-label col-md-2">Nombre de Usuario</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.UsuarioNombre, new { htmlAttributes = new { @class = "form-control", required = "required" } })
                @Html.ValidationMessageFor(model => model.UsuarioNombre, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group mt-2">
            <label class="control-label col-md-2">Correo Electrónico</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.Correo, new { htmlAttributes = new { @class = "form-control", type = "email", required = "required" } })
                @Html.ValidationMessageFor(model => model.Correo, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group mt-2">
            @Html.Label("Contraseña", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.Password("Contraseña", null, new { @class = "form-control", required = "required" })
                @Html.ValidationMessage("Contraseña", "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group mt-2">
            <label class="control-label col-md-2" for="RepetirContraseña">Repetir Contraseña</label>
            <div class="col-md-10">
                <input type="password" name="RepetirContraseña" id="RepetirContraseña" class="form-control" required />
            </div>
        </div>

        <div class="form-group mt-4">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Registrarse" class="btn btn-primary" />
                @Html.ActionLink("Volver al Login", "Login", null, new { @class = "btn btn-secondary ms-2" })
            </div>
        </div>
    </div>
}
